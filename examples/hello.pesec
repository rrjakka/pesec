mutab point_new = fn(a, b) {
    struct { x=a, y=b };
};

mutab p1 = point_new(12, 34);
mutab p2 = point_new("Hello", "World");

mutab point_print = fn(p) {
    println("{", p.x, ", ", p.y, "}");
};

point_print(p1);
point_print(p2);